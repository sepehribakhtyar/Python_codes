# Reshape vectors as column vectors:
import numpy as np
Ytrain=Ytrain.reshape(len(Ytrain),1)
Ytest=Ytest.reshape(len(Ytest),1)
Ytrain_pred=Ytrain_pred.reshape(len(Ytrain_pred),1)
Ytest_pred=Ytest_pred.reshape(len(Ytest_pred),1)













